---
export interface Props {
  href: string;
  label: string;
  icon?: string; // Optional icon class
}

const { href, label, icon } = Astro.props;
---

<li>

  <a href={href} class="flex items-center p-2 text-gray-900 rounded-lg dark:text-black group">

    <div>
      {icon ? <i class={icon + " flex-shrink-0 w-5 h-5 text-gray-500 dark:text-black "} aria-hidden="true"></i> :
      <svg class="" aria-hidden="true">
        
      </svg>}
    </div>
    <span class="flex-1 ms-3 whitespace-nowrap pl-3">
      {label}
    </span>
  </a>
</li